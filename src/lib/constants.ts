// Cash reconciliation threshold in VND
// Increase for users with many small transactions that may accumulate rounding differences
export const CASH_DRIFT_THRESHOLD_VND = 100;

export const QUOTE_REFRESH_INTERVAL_MS = 15000;
export const MAX_HEADER_SCAN_ROWS = 50;
